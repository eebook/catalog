{
    "name": "github",
    "repo": "https://github.com/eebook/githubeebook",
    "info": "Convert github issue to ebook",
    "regex": "(?<=github\\.com/)",
    "schema": {
        "properties": {
            "token": {
                "type": "string",
                "description": "github token"
            }
        },
        "required": ["token"]
    }
}
